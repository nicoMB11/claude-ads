# One site-config.yml per site. Written by hand (Option A) or generated by
# `mb-rebuild detect` (Option B), which keeps ONLY catalogued extensions and
# flags anything unknown as a potential nulled plugin.

site: poeles-cheminees.com
plugins:
  - elementor
  - elementor-pro
  - wp-seopress-pro
  - woocommerce
theme: hello-elementor
php: "8.2"
